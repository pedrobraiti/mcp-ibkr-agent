# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / environment
.env
*.local

# Data / logs / DB
data/
logs/
*.db
*.log

# Demo source video — only the optimized assets/demo.gif is committed (keep repo light)
assets/*.mp4

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent working memory: internal journals (account id, balances, "where I left off").
# Kept local — out of the public repo. CLAUDE.md (at the root) stays versioned.
.claude/
